# S2-02 测试报告

## 1. 门禁状态

```toml
related_regression_gate = "missing"
canonical_benchmark_gate = "missing"
postflight_gate = "missing"
```

本报告只记录真实执行结果。未实际运行的 pytest、canonical benchmark 或 postflight 不记为 pass。

## 2. 已验证

### 静态范围

- `get_caller_info()` Interface 未改变。
- `create_log_line() -> get_caller_info()` Seam 未改变。
- 生产差量仅位于 `src/custom_logger/formatter.py`。
- `logger.py`、`config.py`、writer/queue writer 均未修改。
- `test_tc0019_call_chain.py` 最终仅改变异常注入点：`inspect.stack` -> `inspect.currentframe`。
- raw frame 不进入 list/tuple/闭包/全局/返回值；`frame/current_frame` 在 `finally` 显式删除。

### supplementary performance diagnostic

该结果来自主 Agent 当前 Linux 临时环境，不是冻结的 Windows canonical benchmark：

| Case | baseline median ns/call | candidate median ns/call | candidate / baseline |
|:---|---:|---:|---:|
| A | 64,261.1259 | 1,724.7074 | 0.026839 |
| B | 72,622.8086 | 1,919.3409 | 0.026429 |

诊断协议为 7 rounds × 10,000 次，两个 Case 均为 candidate < baseline。该结果仅作方向性补证，不满足 AC-S2-02-03 的正式次数与解释器要求。

## 3. 未执行的硬门禁

### 聚焦 pytest

未执行。冻结命令：

```text
D:/anaconda3/envs/base_python3.12/python.exe -m pytest tests/01_unit_tests/test_s2_02_caller_info.py tests/01_unit_tests/test_tc0019_call_chain.py -v -p no:cacheprovider
```

### 历史只读回归

未执行。冻结命令：

```text
D:/anaconda3/envs/base_python3.12/python.exe -m pytest tests/test_custom_logger/test_tc0022_dynamic_module_name.py tests/test_custom_logger/test_tc0021_error_stack_trace.py -v -p no:cacheprovider
```

### 全量回归

未执行。冻结命令：

```text
D:/anaconda3/envs/base_python3.12/python.exe -m pytest tests/ -v -p no:cacheprovider
```

### canonical benchmark

未执行。必须使用 `D:/anaconda3/envs/base_python3.12/python.exe`，baseline `1e6f7c19c6666ec988efb70633166127479e2007` 与 candidate 在同机同解释器下分别运行 `10,000` warmup、`7 × 100,000`，Case A/B candidate median 均严格低于 baseline。

## 4. 环境阻塞事实

- 当前主 Agent 临时执行环境为 Linux，不具备冻结的 Windows canonical interpreter。
- 环境无法 DNS 访问 GitHub，不能 `git clone` 当前 issue 分支。
- 本地没有完整当前仓库运行环境，且缺少项目外部依赖 `is_debug` / `config_manager`。
- 用户明确不使用 GitHub Actions，因此没有 CI 替代结果。

## 5. 结论

代码与静态范围符合冻结设计；方向性性能诊断通过。由于 related regression、canonical benchmark、postflight 未真实完成，本测试报告结论为 **missing / blocked**，不能用于推进待验收。
