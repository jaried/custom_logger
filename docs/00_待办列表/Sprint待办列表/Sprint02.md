# Sprint02 待办列表

**Sprint周期**：2026-08-02 ~ 待定
**Sprint目标**：承接 Sprint01 遗留的日志路径输出格式需求

## Sprint 状态：进行中

## Sprint 跟踪

| ID | 内容 | 状态 | 优先级 | 估算 | 依赖 | 冲突 | Phase |
|:---|:---|:---|:---|:---|:---|:---|:---|
| S2-01 | [US] 初始化日志目录输出追加目录分隔符 | 待办 | P2 | 1 | - | - | - |

## Issue 详情

### S2-01 [US] 初始化日志目录输出追加目录分隔符

> 作为开发者，我希望初始化成功后的日志目录信息末尾带目录分隔符，以便清晰区分目录路径与日志文件名。

**关联issue**：Sprint01/US-003 [相关]
**关联说明**：在 Sprint01/US-003 初始化日志路径提示基础上，补充目录末尾分隔符要求。

**涉及文件**：
- src/custom_logger/manager.py
- tests/01_unit_tests/test_tc0030_init_log_path.py

**待澄清歧义**（登记阶段仅识别记录，需求澄清由 requirements-clarification 承接）：
- [歧义1] 分隔符是否必须使用运行平台原生分隔符 | 可选解释：A. 使用 `os.sep` 对应的平台分隔符 / B. 固定允许 `\\` 或 `/` 任一形式 | 推荐：A
- [歧义2] 输入目录已经带分隔符时是否禁止重复 | 可选解释：A. 保证恰好一个结尾分隔符 / B. 仅要求至少一个结尾分隔符 | 推荐：A
- [歧义3] 该格式约束是否仅适用于 US-003 的初始化提示 | 可选解释：A. 仅初始化提示 / B. 所有日志目录输出 | 推荐：A

**数据闭环**：不涉及

**结构约束**：
- 结构原则：低耦合高内聚代码复用
- 当前事实：用户要求调整初始化路径提示的输出格式；已知实现与测试边界分别位于 `src/custom_logger/manager.py` 和 `tests/01_unit_tests/test_tc0030_init_log_path.py`；详细拆分与方案评估由 solution-decision 承接。

**验收标准**：
- [ ] 初始化成功后的日志目录输出以 `\\` 或 `/` 结尾
- [ ] 日志目录与 `full.log`、`warning.log` 文件名信息仍同时输出
- [ ] 端到端测试必须通过

**估算点数**：1
**优先级**：P2
