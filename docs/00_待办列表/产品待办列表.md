# 产品待办列表

## 需求类（US）

### US-001 增加log有效期配置并自动删除过期日志

> 作为系统管理员，我希望能够配置日志文件的有效期（保留天数），在首次初始化logger系统时，自动删除超过有效期的日志文件，以便控制日志存储空间占用。

**状态**：待验收

**关联issue**：无

**验收标准**：
- [ ] 新增配置项：日志有效期（天数），可通过config对象配置
- [ ] 初始化时扫描日志目录，识别过期日志文件
- [ ] 自动删除超过有效期的日志文件
- [ ] 清理操作在系统初始化阶段自动执行
- [ ] 记录清理操作日志（删除了哪些文件、释放多少空间）
- [ ] 端到端测试必须通过（禁止跳过端到端测试）

**估算点数**：5
**优先级**：中
**创建时间**：2026-02-02

## 优化类（OPT）

### OPT-001 统一warning及以上级别记录调用栈

> 作为开发者，我希望warning及以上级别的日志都能记录调用栈，以便更好地追踪问题来源。

**状态**：进行中

**验收标准**：
- [ ] warning级别记录调用栈
- [ ] error级别记录调用栈
- [ ] critical级别记录调用栈
- [ ] exception级别记录调用栈
- [ ] 现有行为保持兼容（exception/error/critical原有调用栈功能正常）
- [ ] 端到端测试必须通过（禁止跳过端到端测试）

**估算点数**：3
**优先级**：中
**创建时间**：2026-01-29

## 任务类（TASK）

### TASK-001 重构docs目录为敏捷开发结构

> 作为开发者，我希望将docs目录重构为标准敏捷开发结构，以便更好地管理Sprint和项目文档。

**状态**：待办

**验收标准**：
- [ ] 旧文档（除00_待办列表外）移动到99_归档/
- [ ] 创建01_Sprint记录/目录
- [ ] 创建02_架构决策记录/目录
- [ ] 创建03_架构文档/目录
- [ ] 创建04_完成定义/目录
- [ ] 创建05_预研/目录
- [ ] 端到端测试必须通过（禁止跳过端到端测试）

**估算点数**：2
**优先级**：高
**创建时间**：2026-01-29
